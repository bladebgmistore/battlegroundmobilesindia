[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"

# Do NOT add catch-all redirects for Next.js.
# The Next.js runtime plugin handles routing automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"
