import UserAuthPage from "@/components/user-auth";

export const metadata = { title: "Sign In | Battleground India Store" };
export default function Page() {
  return <UserAuthPage mode="login" />;
}
