import UserAuthPage from "@/components/user-auth";

export const metadata = { title: "Create Account | Battleground India Store" };
export default function Page() {
  return <UserAuthPage mode="signup" />;
}
