import UserAuthPage from "@/components/user-auth";

export const metadata = { title: "Forgot Password | Battleground India Store" };
export default function Page() {
  return <UserAuthPage mode="forgot" />;
}
