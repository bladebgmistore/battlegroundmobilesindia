# Copy this file to `.env` and fill in your database connection string.
# On Netlify: Site configuration -> Environment variables -> add DATABASE_URL
#   Scope: All scopes (Builds, Functions, Prerender)  ->  then re-deploy.
DATABASE_URL=postgresql://USER:PASSWORD@HOST.region.aws.neon.tech/DBNAME?sslmode=require
